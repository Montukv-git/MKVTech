This patch updates `src/Portfolio.tsx` to the full version and fixes the resume path for GitHub Pages.
Upload/replace the file at the same path in your repo, then the GitHub Action will rebuild automatically.
